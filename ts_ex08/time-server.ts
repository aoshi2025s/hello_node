{
	/*
	 * allowed functions: all
	 * the server should listen to TCP connections on the port provided
	 * by the first argument
	 * "YYYY-MM-DD hh:mm"
	 * after sending the string, close the connection
	 */
}
