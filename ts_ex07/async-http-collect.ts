{
	/*
	 * (HTTP Collect) in that you need to use http.get()
	 * URLs as the first three command-line arguments
	 * collect the complete content provided to you by each
	 * of the URLs and print it to the console.
	 * one line per URL
	 */

}
